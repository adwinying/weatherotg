package pages

import (
	"github.com/adwinying/weatherotg/lib"
)

templ IndexContent(location string, weatherInfo *lib.WeatherInfo) {
	<div>
		<h1 class="text-xl">Weather in { location }</h1>
		<p>Temperature: { weatherInfo.CurrentCondition[0].TempC }°C</p>
	</div>
}
